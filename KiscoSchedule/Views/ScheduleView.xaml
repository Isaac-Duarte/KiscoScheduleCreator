<UserControl x:Class="KiscoSchedule.Views.ScheduleView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro.Platform" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:KiscoSchedule.Views"
             xmlns:vm="clr-namespace:KiscoSchedule.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal" 
             TextOptions.TextRenderingMode="Auto"        
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <StackPanel>
        <TextBlock Text="{Binding Month}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center" />
        <DataGrid ItemsSource="{Binding Employees}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee" Binding="{Binding Name}" IsReadOnly="True" />
                <DataGridComboBoxColumn Header="Sunday" ItemsSource="{Binding Shifts}" DisplayMemberPath="{Binding Key}"/>
                <DataGridComboBoxColumn Header="Monday" ItemsSource="{Binding Shifts}" />
                <DataGridComboBoxColumn Header="Tuesday" ItemsSource="{Binding Shifts}" />
                <DataGridComboBoxColumn Header="Wednesday" ItemsSource="{Binding Shifts}" />
                <DataGridComboBoxColumn Header="Thursday" ItemsSource="{Binding Shifts}" />
                <DataGridComboBoxColumn Header="Friday" ItemsSource="{Binding Shifts}" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
