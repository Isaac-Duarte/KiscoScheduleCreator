<UserControl x:Class="KiscoSchedule.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KiscoSchedule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">
        <TextBlock Text="Twilio API Key" Style="{StaticResource MaterialDesignHeadline5TextBlock}" />
        <TextBox Text="{Binding Path=TwilioApiKey, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
</UserControl>
